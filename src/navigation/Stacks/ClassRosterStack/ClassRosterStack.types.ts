
export type ClassRosterStackParamList = {
    CourseClass: {courseId:string|Number}; // No parameters for this screen
    ClassRoster: { classId: string }; // ClassRoster expects a classId parameter
  };